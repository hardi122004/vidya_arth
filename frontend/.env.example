VITE_API_URL=http://localhost:8000

# Optional: enables live YouTube video results on Skill Detail pages.
# Without a key, the UI falls back to a "Search on YouTube" link instead of
# fabricating fake video results. Get a key from Google Cloud Console
# (YouTube Data API v3).
VITE_YOUTUBE_API_KEY=
